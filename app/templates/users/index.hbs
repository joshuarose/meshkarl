<h3>All Users:</h3>

<table class="table table-striped">
  <thead>
    <th>Name</th>
    <th>Email</th>
  </thead>
  <tbody>
    {{#each model as |user|}}
      <tr>
        <td>{{user.name}}</td>
        <td>{{user.email}}</td>
      </tr>
      {{/each}}
  </tbody>
</table>


{{outlet}}
